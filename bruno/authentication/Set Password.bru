meta {
  name: Set Password
  type: http
  seq: 2
}

post {
  url: http://localhost:8080/authentication/set-password
  body: json
  auth: inherit
}

headers {
  Authorization: {{AUTHORIZATION}}
}

body:json {
  {
    "password": "newpassword"
  }
}

settings {
  encodeUrl: true
}
