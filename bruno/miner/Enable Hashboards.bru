meta {
  name: Enable Hashboards
  type: http
  seq: 7
}

post {
  url: http://localhost:8080/miner/enable-hashboards
  body: json
  auth: inherit
}

headers {
  Authorization: {{AUTHORIZATION}}
}

body:json {
  {
    "saveAction": "SAVE_ACTION_SAVE_AND_APPLY",
    "hashboardIds": ["0", "1"]
  }
}

settings {
  encodeUrl: true
}
