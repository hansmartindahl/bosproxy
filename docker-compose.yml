services:
  bosproxy:
    image: dahlnetorg/bosproxy:latest
    container_name: bosproxy
    environment:
      - GRPC_SERVER_ADDRESS=192.168.20.110
      - GRPC_SERVER_PORT=50051
    ports:
      - "8080:8080"
    restart: unless-stopped
